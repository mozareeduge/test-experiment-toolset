# Field Charter template (spec §7, `FieldCharter`)

- **Research purpose:**
- **Corpus snapshot** (exact commit/manifest hash — never "latest"):
- **Inclusion rule:**
- **Exclusion rule:**
- **Active encounter scales** (verse/couplet/section/poem/category/poet-work/field/token-window):
- **Empirical temporal scope** (state the proxy rule explicitly if derived — spec §11):
- **Empirical spatial/institutional scope** (if relevant):
- **Known blind zones:**
- **Reopening conditions:**

If any scope condition is *derived* rather than native (spec §23.2), label it
"derived" in the researcher-facing summary and show the exact rule used —
never imply a poet-life proxy is a poem composition date.
